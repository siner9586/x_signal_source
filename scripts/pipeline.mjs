import fs from 'node:fs/promises';
import path from 'node:path';
import crypto from 'node:crypto';
import yaml from 'js-yaml';

const ROOT=process.cwd(), DATA=path.join(ROOT,'data'), TZ='Asia/Shanghai';
const WEIGHTS={source_score:.2,information_density_score:.2,originality_score:.15,trend_score:.15,evidence_score:.1,heat_score:.1,site_fit_score:.1};
const TOPICS=['AI Agent','AI Coding','Reasoning Models','Multimodal','AI Search','AI Infra','Open Source','Startups','Product','Investment','Research','Robotics','Safety','中文 AI 观点'];
const BAD=['giveaway','airdrop','meme','follow me','retweet to win','crypto alpha','100x','抽奖','互关','空投','暴富','割韭菜','无脑转发','速成','玄学'];
const ensure=p=>fs.mkdir(p,{recursive:true});
const exists=async p=>!!(await fs.access(p).then(()=>true).catch(()=>false));
const rjson=async(p,d)=>exists(p).then(ok=>ok?fs.readFile(p,'utf8').then(JSON.parse):d);
const wjson=async(p,d)=>{await ensure(path.dirname(p));await fs.writeFile(p,JSON.stringify(d,null,2)+'\n')};
const h=s=>crypto.createHash('sha256').update(String(s||'').trim().toLowerCase()).digest('hex').slice(0,16);
const today=()=>new Intl.DateTimeFormat('en-CA',{timeZone:TZ,year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date());
const now=()=>new Date().toISOString();
const norm=u=>{try{const x=new URL(u);x.hash='';['utm_source','utm_medium','utm_campaign','utm_term','utm_content','ref'].forEach(k=>x.searchParams.delete(k));return x.toString().replace(/\/$/,'')}catch{return u||''}};
const dom=u=>{try{return new URL(u).hostname.replace(/^www\./,'')}catch{return''}};
const yload=async p=>yaml.load(await fs.readFile(p,'utf8'))||[];
function typeOf(url,cat=''){const d=dom(url);if(d.includes('x.com')||d.includes('twitter.com'))return'x_article';if(d.includes('github.com'))return'github_repo';if(d.includes('arxiv.org')||d.includes('openreview.net')||cat.includes('research'))return'paper';if(cat.includes('podcast')||d.includes('youtube.com')||d.includes('spotify'))return'podcast_episode';if(cat.includes('media'))return'media_report';if(cat.includes('company')||cat.includes('official'))return'company_blog';return'external_article'}
function kw(text,words,base){const t=text.toLowerCase();return Math.min(100,base+words.filter(w=>t.includes(String(w).toLowerCase())).length*12)}
function cand(e,date,file){const url=norm(e.url||e.homepage_url||e.x_url||e.blog_url||e.rss_url||'');const title=e.title||e.display_name||e.name||e.organization||dom(url)||'Untitled source';const text=`${title} ${e.notes||''} ${(e.tags||[]).join(' ')}`;const evidence=[e.homepage_url,e.blog_url,e.rss_url,e.github_url,e.youtube_url,e.podcast_url,e.x_url,e.url].filter(Boolean).map(norm);const c={id:`cand_${h(url+title)}`,canonical_url:url,source_url:url,source_platform:dom(url).includes('x.com')?'x':'web',source_domain:dom(url),content_type:typeOf(url,e.category||file),title,subtitle:e.notes||'',author:e.author||e.display_name||e.name||'',author_handle:e.handle||'',author_role:e.role||'',organization:e.organization||e.name||'',language:e.language||'en',published_at:e.published_at||date,captured_at:now(),summary:e.summary||`${title} 是当前信息源库中的高信号公开入口，本期作为初始来源索引收录，用于后续从公开 RSS、博客、GitHub、播客和网页元数据持续发现新内容。`,excerpt:e.notes||'仅索引公开链接，不保存完整正文。',raw_text_available:false,topics:e.topics||TOPICS.filter(t=>text.toLowerCase().includes(t.toLowerCase())).slice(0,3),tags:e.tags||[e.category||'source'],entities:[e.organization||e.name||e.display_name].filter(Boolean),mentioned_companies:e.organization?[e.organization]:[],mentioned_people:e.role?[e.display_name||e.name].filter(Boolean):[],mentioned_products:[],mentioned_papers:[],mentioned_repos:evidence.filter(u=>dom(u).includes('github.com')),evidence_links:[...new Set(evidence)],engagement:{likes:0,reposts:0,replies:0,bookmarks:0,views:0,comments:0,stars:0,score:0},source_score:Math.min(100,45+Number(e.priority||3)*10+((e.category||'').includes('official')?10:0)),information_density_score:kw(text,['research','blog','github','paper','benchmark','docs','deep dive','framework','复盘','方法论'],42),originality_score:kw(text,['official','founder','researcher','maintainer','creator','原创','作者'],38),trend_score:kw(text,['agent','coding','reasoning','multimodal','infra','startup','investment','AI Agent','AI 编程'],45),evidence_score:Math.min(100,45+evidence.length*9),heat_score:40,site_fit_score:kw(text,['AI','agent','coding','startup','research','product','investment','infra','模型','智能体'],50),total_score:0,rank:0,cluster_id:`${(e.category||'source').replace(/[^a-z0-9]+/gi,'_').toLowerCase()}_${h(title).slice(0,8)}`,dedupe_key:h(`${norm(url)}|${title}|${e.handle||''}`),status:'candidate',reason_selected:'',reason_rejected:'',fetch_status:'source_index_only',fetch_error:'',first_seen_issue:date,last_seen_issue:date,used_in_issue:null};c.total_score=Math.round(Object.entries(WEIGHTS).reduce((s,[k,w])=>s+c[k]*w,0));if(!c.topics.length)c.topics=['AI Signal'];return c}
async function sources(){const dir=path.join(DATA,'sources');const excluded=new Set(['query_templates.yaml','blocklist.yaml']);const files=(await fs.readdir(dir).catch(()=>[])).filter(f=>(f.endsWith('.yaml')||f.endsWith('.yml'))&&!excluded.has(f)).sort();const out=[];for(const f of files){const body=await yload(path.join(dir,f));const arr=Array.isArray(body)?body:[];arr.filter(e=>e&&typeof e==='object').forEach(e=>out.push({...e,_source_file:f}))}return out}
async function collect(){const d=today(),entries=await sources();const candidates=entries.map(e=>cand(e,d,e._source_file)).filter(c=>c.canonical_url&&!BAD.some(w=>(c.title+c.summary).toLowerCase().includes(w.toLowerCase())));await wjson(path.join(DATA,'candidates',`${d}.json`),{issue_date:d,generated_at:now(),candidates});return{d,candidates}}
async function score(){const d=today(),p=path.join(DATA,'candidates',`${d}.json`),data=await rjson(p,{issue_date:d,candidates:[]});data.candidates.sort((a,b)=>b.total_score-a.total_score).forEach((c,i)=>c.rank=i+1);await wjson(p,data);return data}
const layer=s=>s>=85?'must_read':s>=70?'worth_reading':s>=55?'signal_watch':s>=40?'archive_only':'rejected';
async function buildIssue(){const d=today(),data=await rjson(path.join(DATA,'candidates',`${d}.json`),{candidates:[]}),usedPath=path.join(DATA,'archive','used_items.json');const raw=await rjson(usedPath,[]),hist=raw.filter(u=>u.issue_date!==d),usedKeys=new Set(hist.flatMap(u=>[u.canonical_url,u.dedupe_key,u.cluster_id,h(u.title||'')].filter(Boolean)));const buckets={must_read:[],worth_reading:[],signal_watch:[],archive_only:[],rejected:[]};let dup=0;const seen=new Set();for(const item of data.candidates){const keys=[item.canonical_url,item.dedupe_key,item.cluster_id,h(item.title)].filter(Boolean);if(keys.some(k=>usedKeys.has(k))||keys.some(k=>seen.has(k))){dup++;item.status='archived';item.reason_rejected='历史或本期重复，按去重规则不再入选。';buckets.archive_only.push(item);continue}keys.forEach(k=>seen.add(k));const l=layer(item.total_score);item.status=l==='rejected'?'rejected':l==='archive_only'?'archived':'selected';item.reason_selected=item.status==='selected'?`核心信号：${item.title} 提供了可追踪的公开入口与证据链，适合观察 ${item.topics.slice(0,2).join(' / ')} 的后续变化。`:'';item.reason_rejected=item.status==='rejected'?'综合评分低于展示阈值或与站点主题匹配不足。':item.reason_rejected;buckets[l].push(item)}buckets.must_read=buckets.must_read.slice(0,8);buckets.worth_reading=buckets.worth_reading.slice(0,16);buckets.signal_watch=buckets.signal_watch.slice(0,20);const selected=[...buckets.must_read,...buckets.worth_reading,...buckets.signal_watch];selected.forEach(i=>i.used_in_issue=d);const clusters=selected.map(i=>({cluster_id:i.cluster_id,title:i.title,summary:i.summary,primary_source:i.canonical_url,supporting_sources:i.evidence_links,related_x_posts:i.evidence_links.filter(u=>dom(u).includes('x.com')),related_articles:i.evidence_links.filter(u=>!dom(u).includes('x.com')),entities:i.entities,tags:i.tags,score:i.total_score,selected_item_id:i.id}));const issue={metadata:{issue_date:d,generated_at:now(),timezone:TZ,sources_scanned:(await sources()).length,candidates_count:data.candidates.length,selected_count:selected.length,duplicates_blocked:dup,fetch_failures:data.candidates.filter(c=>c.fetch_status==='failed').length,initial_source_index:true},summary:{one_liner:'本期为初始来源索引：先建立可合规追踪的 X 与外部高信号入口，再由每日流水线增量筛选新内容。',trends:['AI Agent','AI Coding','AI Infra','Research','Startup / Investment'],watch_next:['公开 RSS 与博客新增内容','GitHub releases 与 trending','人工维护 X Article / Thread seed list','播客长访谈中的一手判断']},must_read:buckets.must_read,worth_reading:buckets.worth_reading,signal_watch:buckets.signal_watch,hot_rank:selected.slice().sort((a,b)=>b.heat_score-a.heat_score).slice(0,20),clusters,sources:[...new Set(selected.map(i=>i.source_domain))]};await wjson(path.join(DATA,'issues',`${d}.json`),issue);await ensure(path.join(ROOT,'content','issues'));await fs.writeFile(path.join(ROOT,'content','issues',`${d}.md`),`---\ndate: ${d}\ntitle: X Signal Source ${d}\n---\n\n${issue.summary.one_liner}\n`);await wjson(usedPath,[...hist,...selected.map(i=>({item_id:i.id,canonical_url:i.canonical_url,dedupe_key:i.dedupe_key,title:i.title,issue_date:d,cluster_id:i.cluster_id,used_as:i.status}))]);return issue}
async function siteData(){await ensure(path.join(ROOT,'public','index-data'));const dir=path.join(DATA,'issues'),files=(await fs.readdir(dir).catch(()=>[])).filter(f=>f.endsWith('.json')).sort(),idx=[];for(const f of files){const x=await rjson(path.join(dir,f),null);if(x)idx.push({date:x.metadata.issue_date,selected_count:x.metadata.selected_count,candidates_count:x.metadata.candidates_count,must_read_count:x.must_read.length,generated_at:x.metadata.generated_at})}const latestDate=idx.at(-1)?.date||today(),latest=await rjson(path.join(dir,`${latestDate}.json`),null);await wjson(path.join(ROOT,'public','index-data','latest.json'),latest||{});await wjson(path.join(ROOT,'public','index-data','issues.json'),idx.reverse());const search=latest?[...latest.must_read,...latest.worth_reading,...latest.signal_watch].map(i=>({title:i.title,url:i.canonical_url,summary:i.summary,tags:i.tags,author:i.author,source:i.source_domain,date:latest.metadata.issue_date,score:i.total_score})):[];await wjson(path.join(ROOT,'public','index-data','search.json'),search)}
async function qa(){const req=['package.json','README.md','astro.config.mjs','.github/workflows/daily.yml','data/sources/source_policy.md','src/pages/index.astro','src/pages/sources.astro','src/pages/about.astro'];const errors=[];for(const r of req)if(!(await exists(path.join(ROOT,r))))errors.push(`missing ${r}`);const latest=await rjson(path.join(ROOT,'public','index-data','latest.json'),null);if(latest){const selected=[...(latest.must_read||[]),...(latest.worth_reading||[]),...(latest.signal_watch||[])];if((latest.metadata?.selected_count||0)!==selected.length)errors.push('selected_count mismatch');const urls=new Set(),keys=new Set();for(const i of selected){for(const k of ['title','canonical_url','summary','reason_selected','total_score','dedupe_key','content_type'])if(i[k]===undefined||i[k]===null||i[k]==='')errors.push(`item ${i.id} missing ${k}`);if(urls.has(i.canonical_url))errors.push(`duplicate url ${i.canonical_url}`);urls.add(i.canonical_url);if(keys.has(i.dedupe_key))errors.push(`duplicate key ${i.dedupe_key}`);keys.add(i.dedupe_key)}}const wf=await fs.readFile(path.join(ROOT,'.github/workflows/daily.yml'),'utf8').catch(()=>'');if(!wf.includes('12 22 * * *'))errors.push('cron is not 12 22 * * *');const readme=await fs.readFile(path.join(ROOT,'README.md'),'utf8').catch(()=>'');if(!readme.includes('不使用付费 API')||!readme.includes('合规边界'))errors.push('README compliance missing');if(errors.length){console.error(errors.join('\n'));process.exit(1)}console.log('QA passed')}
async function daily(){await collect();await score();await buildIssue();await siteData();await qa()}
await ensure(path.join(ROOT,'content','issues'));
const cmd=process.argv[2]||'daily';
if(cmd==='collect')await collect();else if(cmd==='score')await score();else if(cmd==='build-issue')await buildIssue();else if(cmd==='generate-site-data')await siteData();else if(cmd==='qa')await qa();else if(cmd==='daily')await daily();else throw new Error(`Unknown command ${cmd}`);
