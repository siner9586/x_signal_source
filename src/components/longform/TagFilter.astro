---
const { tags = [] } = Astro.props;
---
<div class="lf-actions" id="tagFilter">
  <button class="lf-btn primary" type="button" data-tag="all">All</button>
  {tags.map((tag) => <button class="lf-btn" type="button" data-tag={tag}>{tag}</button>)}
</div>
