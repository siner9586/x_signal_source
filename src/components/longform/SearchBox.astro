---
const { id = 'longformSearch', placeholder = 'Search title, source, guest, tags, full transcript...' } = Astro.props;
---
<input class="lf-searchbar" id={id} type="search" placeholder={placeholder} autocomplete="off" />
