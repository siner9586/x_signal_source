---
const { placeholder = 'Search title, source, guest, tags, full transcript...' } = Astro.props;
---
<input class="lf-searchbar" id="longformSearch" type="search" placeholder={placeholder} autocomplete="off" />
