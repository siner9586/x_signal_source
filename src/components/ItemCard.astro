---
import SignalCard from './SignalCard.astro';
const { item, compact = false, variant } = Astro.props;
const resolvedVariant = variant || (compact ? 'compact' : 'featured');
---
<SignalCard item={item} variant={resolvedVariant} />
